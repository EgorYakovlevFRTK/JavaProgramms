package swingGuiProg;

import javax.swing.JFrame;

public class Window extends JFrame { //Наследуем JFrame: расширяем класс
	
	public Window () {
		
		setVisible(true);
		setLocationRelativeTo(null);
		setSize(400, 400);
		setDefaultCloseOperation(EXIT_ON_CLOSE);
		
	}
}
